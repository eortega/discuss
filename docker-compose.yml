---
version: '2'
services:
  app:
    image: phoenix:1.2.5
    build: .
    ports:
      - "4001:4000"
    volumes:
      - ./:/app
    depends_on:
      - db
  db:
    image: postgres:9
    volumes:
      - /usr/local/var/data/postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
